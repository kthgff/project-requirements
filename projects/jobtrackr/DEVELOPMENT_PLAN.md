# DEVELOPMENT_PLAN.md

## Project Overview
Build and ship JobTrakr quickly with clean, maintainable implementation. This plan is now execution-first: use locked PM decisions as the product source of truth, minimize new requirements work, and prioritize coding the smallest end-to-end slices.


## Execution Rule

From this point forward, engineering should prefer coding against locked PM semantics over opening new requirement questions unless blocked.

Use these as locked product truths:
- Google login is required
- dashboard is the main jobs page
- jobs page is a searchable database-backed table
- each job has a match percentage
- low-fit, strong-fit, fit-ready, and pending-fit are fit signals, not workflow states
- TypeScript is the project language direction
- Gmail discovery may use the `gog` CLI for local/operator-driven search and debug ingestion
- source emails are first-class records and must be persisted before or alongside parsed job creation

If a requirement appears ambiguous, check the canonical PM specs first and escalate only if still blocked.

## Team
| Agent | Role | Channel |
|---|---|---|
| Frank | Lead SWE | #swe-frank |
| Alice | SWE | #swe-alice |
| Marcus | SWE | #swe-marcus |
| Priya | SWE | #swe-priya |

## Active Tasks
| ID | Task | Owner | Status | Branch | Blocked By | DoD | AC | Gap |
|---|---|---|---|---|---|---|---|---|
| T-001 | Finalize PM decision alignment in JobTrackr API contract and debug semantics | Marcus | in-progress | chore/jobtrackr-contract-alignment | — | PM decisions documented, API contract finalized, debug semantics defined, spec approved | All open PM questions resolved, API contract versioned, debug semantics documented, spec reviewed by PM | — |
| T-002 | Build local Go web app for Gmail inbox job search, protected shell, and raw source-email ingestion | Alice | in-progress | feat/gmail-job-search-webapp | — | Local app runs, Gmail search works, protected shell implemented, raw ingestion works, tested | Local web app starts without errors, Gmail inbox search returns emails, protected routes require auth, raw emails stored in DB | — |
| T-003 | Produce JobTrakr dependency map and milestone sequencing pass for parallel-safe delivery | Frank | in-progress | chore/jobtrackr-dependency-map | — | Dependency map created, sequencing defined, parallel-safe validated, documented | Dependency graph complete, milestone sequence validated for parallel execution, blockers identified, documented in spec | — |
| T-004 | Lock jobs dashboard contract for match rating, flagged semantics, and table payload shape | Marcus | in-progress | feat/jobtrackr-jobs-dashboard-contract | — | Match rating defined, flagged semantics defined, table payload locked, API documented, reviewed | Match rating algorithm defined, flag thresholds documented, table JSON schema locked, OpenAPI spec updated | — |
| T-005 | Scaffold protected jobs dashboard web shell with mock table data for the first vertical slice | Priya | in-progress | feat/jobs-dashboard-mock-shell | — | Dashboard scaffolded, mock data displayed, auth works, responsive, loading states | Dashboard page loads with auth, table displays mock jobs, responsive on mobile/tablet, loading spinner shows | — |
| T-006 | Implement canonical low-fit handling for jobs with fit score below 60 without introducing non-canonical workflow statuses | Frank | in-progress | feat/jobtrackr-auto-close-logic | — | Low-fit handling is implemented against the canonical workflow model, low-score jobs remain representable without a fake status transition, tested, and edge cases are handled | Jobs with score < 60 are surfaced as low-fit according to the canonical fit contract, score = null does not force a workflow change, and logging/debug output is added | Reworded under T-033 to remove stale `not a match` workflow semantics and point implementation back to the canonical Gate A rules |
| T-007 | Implement view all jobs page with full job details and filtering | Alice | in-progress | feat/jobtrackr-view-all-jobs-page | — | Page created, details displayed, filtering works, search works, pagination works, tested | Page shows all jobs, filters by status/fit/date work, search returns results, pagination navigates pages | — |
| T-008 | Implement job details page to view full job information (description, skills, tags, notes, source link) | Marcus | in-progress | feat/jobtrackr-job-details-page | — | Details page created, all fields displayed, source link works, notes editable, tags manageable | Page shows all required fields, source link opens in new tab, notes save to DB, tags add/remove works | — |
| T-009 | Polish jobs table interaction states and shared dashboard UX for the mock-data frontend slice | Priya | in-progress | feat/jobtrackr-dashboard-table-polish | — | Dashboard and jobs views share richer table states, selected row treatment is clear, and empty/loading shells are present | Mock-data frontend supports obvious row selection, empty-state rendering, loading placeholders, and smoother dashboard-to-jobs navigation | Claimed from unassigned frontend polish work after Jimmy's latest sequencing pass |
| T-010 | Break the approved dependency sequencing into implementation-ready engineering tickets for Milestones 1 to 4 | Frank | in-progress | chore/jobtrackr-milestone-ticket-breakdown | T-001, T-004 | Milestones 1 to 4 have implementation-ready tickets with dependencies, owners, and exit criteria documented | Each ticket traces to a milestone step, notes contract dependencies, and gives engineering enough detail to implement without PM follow-up | Claimed from unassigned engineering handoff follow-through after Jimmy's latest sequencing pass |
| T-011 | Improve the responsive jobs list UX with mobile cards, clearer active filter context, and compact results navigation | Alice | in-progress | feat/jobtrackr-responsive-job-list | — | Jobs page works cleanly on smaller screens, active filters are visible, and list navigation remains usable while reviewing jobs | Mobile layout shows readable job cards, filter state is obvious, and users can move through filtered results without losing context | Claimed from unassigned jobs-page UX follow-through after Jimmy's latest pickup guidance |
| T-012 | Add editable notes and tag management interactions to the job details page using the agreed detail-view contract | Marcus | in-progress | feat/jobtrackr-job-details-editing | T-008 | Notes can be edited in-place, tags can be added and removed in the detail view, mock-state interactions are documented, and the UI is ready for API wiring | Users can edit notes without leaving the page, add/remove tags with clear controls, and the interaction pattern matches the JobTrackr detail-page contract | Claimed from unassigned detail-view follow-through after Jimmy's latest pickup guidance |
| T-013 | Add visible active-filter context and empty-result recovery controls to the mock jobs workspace | Priya | in-progress | feat/jobtrackr-filter-context | — | Jobs workspace shows the currently active filters, empty-result states explain why nothing matched, and users can recover with a one-click reset or dashboard return path | Active search/status/location filters remain visible after apply, zero-result states render guidance instead of a blank table, and recovery actions clear filters or return to the dashboard without confusion | Claimed from unassigned frontend UX follow-through after Jimmy's latest pickup guidance |
| T-014 | Break the finalized parser and ingestion contract changes into implementation-ready tickets after the jobs dashboard payload is locked | Frank | in-progress | chore/jobtrackr-milestone-ticket-breakdown | T-001, T-004 | Parser and ingestion follow-through is decomposed into concrete engineering tickets with dependencies, delivery order, and acceptance notes | Engineering can pick up Gmail ingestion follow-through work without extra PM clarification once the contract is stable, and each ticket traces to Milestone 4 delivery | Claimed from the unassigned upcoming engineering handoff follow-through during hourly kickoff |
| T-015 | Define the job detail view contract for field grouping, edit interactions, and API expectations across T-008 and T-012 | Marcus | in-progress | feat/jobtrackr-detail-view-contract | T-001, T-004 | Detail-view contract documents required fields, section order, notes and tag edit behavior, and API dependencies for the frontend slice | T-008 and T-012 can implement against one shared detail-view contract without inventing new interaction patterns, and open API dependencies are explicitly called out | Claimed from unassigned detail-view contract follow-through during hourly kickoff |
| T-016 | Define the mock jobs workspace UX contract for active-filter visibility, empty-result recovery, and dashboard-to-jobs navigation | Priya | in-progress | feat/jobtrackr-workspace-ux-contract | T-005, T-009, T-013 | Workspace UX contract documents filter summary treatment, zero-result recovery actions, row-selection persistence, and responsive navigation expectations for the mock-data frontend slice | Priya can implement T-013 and keep T-005/T-009 aligned with one shared interaction contract, with clear states for mobile/tablet and no ambiguity around filter reset or return-to-dashboard flows | Claimed from unassigned frontend workspace follow-through during hourly kickoff |
| T-017 | Close Gate A documentation by reconciling schema, workflow/archive semantics, fit nullability, and source-email linkage across canonical specs | Frank | in-progress | chore/jobtrackr-gate-a-closeout | T-001 | Canonical specs agree on workflow/archive semantics, fit nullability, and one-source-email-to-many-jobs linkage, with Gate A references updated for engineering handoff | Engineering can use the PM memo, schema specs, and milestone tickets without conflicting status enums, archive behavior, fit defaults, or source-email linkage assumptions | Claimed from Jimmy's latest hourly guidance during kickoff |
| T-018 | Implement the Google auth callback, session flow, and Gmail readonly connection path for the first end-to-end vertical slice | Alice | in-progress | feat/jobtrackr-auth-session-gmail-connect | — | Backend exposes auth start/callback/session/logout and Gmail connect/account endpoints for the readonly MVP flow, the app can create a session after Google callback, and the connection state is visible to the frontend | A user can start Google auth, complete callback, receive an app session, and see Gmail readonly connection status without relying on manual token env injection | Claimed from Jimmy's latest hourly guidance during kickoff |
| T-019 | Align the job details UI with the locked detail-view contract, including section order, editing affordances, and list/detail behavior examples | Marcus | in-progress | feat/jobtrackr-job-details-editing | T-015 | Job details UI follows the agreed header, fit/workflow, job content, notes, and tags structure, with notes save/cancel and direct tag editing matching the shared contract | The jobs list side panel and full detail page present the same section order, notes editing requires explicit save/cancel, tags are editable in-context, and mock interactions are documented clearly enough for later API wiring | Claimed from Jimmy's latest hourly guidance during kickoff |
| T-020 | Lock row-selection continuity rules for the mock jobs workspace across filter changes, sorting, and dashboard return flows | Priya | in-progress | feat/jobtrackr-row-selection-continuity | T-016 | Workspace contract and QA cases define when selection is preserved, cleared, restored, and announced across table, detail, and dashboard transitions | Frontend can implement one deterministic selection model for desktop, tablet, and mobile, with explicit recovery behavior when a selected row leaves the visible result set | Claimed from Jimmy's latest hourly guidance during kickoff |
| T-021 | Assemble the final Phase 3 engineering handoff package with source-of-truth links, dependency gates, owner mapping, and immediate pickup guidance | Frank | in-progress | chore/jobtrackr-final-handoff-package | T-017, T-010, T-014 | Engineering has one package that points to the canonical contracts, documents Gate A and Gate B readiness, summarizes milestone tickets, and tells each lane what to pick up next without PM reconstruction | A new engineer can open one handoff file, see the correct source-of-truth docs, understand which contracts are locked vs pending, and know the next recommended task by lane | Claimed during hourly kickoff after Jimmy's 04:02 plan highlighted missing single-file handoff context |
| T-022 | Wire the authenticated frontend shell to the new auth session and Gmail connection endpoints for the first vertical slice | Alice | in-progress | feat/jobtrackr-auth-session-gmail-connect | T-018 | Frontend can start Google auth, reflect signed-in session state, show Gmail connection status, and offer logout/reconnect flows against the new backend endpoints | A user landing in the app can start auth from the UI, see whether a session exists, view Gmail readonly connection state, and reconnect or sign out without manual API calls | Claimed during hourly kickoff to support Alice's auth/session/Gmail vertical slice delivery |
| T-023 | Add canonical list-to-detail contract examples so the jobs workspace and detail UI stay aligned with the locked section order and edit model | Marcus | in-progress | feat/jobtrackr-detail-contract-examples | T-004, T-015 | One shared examples doc shows how the jobs list payload, selected-row state, and detail response work together across drawer and full-page detail patterns, with notes/tag/status interactions matching the locked contract | Frontend and API can point to concrete list/detail examples instead of inferred behavior, and QA can validate section order, selection continuity, and edit affordances from one canonical reference | Claimed during hourly kickoff after Jimmy asked Marcus to push list/detail contract examples and detail editing alignment |
| T-024 | Add canonical workspace session-state examples for deterministic row-selection continuity across filters, sorting, and dashboard return flows | Priya | in-progress | feat/jobtrackr-workspace-session-state | T-016, T-020 | Workspace docs and QA coverage show exact session-state transitions for preserve, clear, and restore behavior across desktop, tablet, and mobile flows | Frontend can implement one session-state model without guessing when to keep `selectedJobId`, when to clear it, how to restore it after filters relax, or what helper copy to show after dashboard re-entry | Claimed during hourly kickoff after Jimmy asked Priya to lock the deterministic selection model across filter changes, sorting, dashboard return flows, and mobile/tablet states |
| T-025 | Publish a canonical Gate A reconciliation matrix so engineering can resolve workflow/archive, fit nullability, and source-email linkage drift from one file | Frank | in-progress | chore/jobtrackr-gate-a-reconciliation-matrix | T-017 | One canonical reconciliation doc maps each Gate A decision to the PM memo, schema, API contract, milestone tickets, and handoff package, with explicit callouts where older assumptions were corrected | Engineers can trace every Gate A rule to the current source-of-truth docs without re-reading the whole repo, and stale workflow/archive semantics are clearly marked as superseded | Claimed during hourly kickoff after Jimmy flagged ongoing contract-drift risk across schema, workflow semantics, and engineering handoff references |
| T-026 | Add persisted Gmail search results and first-pass server-side filtering endpoints for the inbox job search slice | Alice | in-progress | feat/jobtrackr-gmail-search-persistence-filtering | — | API persists Gmail-derived jobs and exposes filtered list retrieval for the first search workflow, with documented query params and tested filter behavior | A developer can run a sync, call one jobs endpoint with status/search/company filters, and receive deterministic persisted results without rescanning Gmail for every view refresh | Claimed from the unassigned upcoming Gmail job search follow-through during hourly kickoff after Jimmy prioritized the auth/session/Gmail vertical slice |
| T-027 | Canonicalize the list-to-detail example docs so API, frontend, and QA reference one authoritative detail continuity example set | Marcus | in-progress | feat/jobtrackr-list-detail-canonicalization | T-015, T-023 | Duplicate or stale list/detail example docs are reconciled, the API contract points to one canonical file, and older references clearly defer to the authoritative example set | Engineers and QA can follow one list/detail example doc without conflicting status examples, edit-flow guidance, or cross-reference drift between API and detail-view contracts | Claimed during hourly kickoff after Jimmy asked Marcus to keep list-to-detail examples canonical and ahead of implementation ambiguity |
| T-028 | Add a canonical workspace continuity QA matrix for filter, sort, dashboard return, and responsive selection behavior | Priya | in-progress | feat/jobtrackr-workspace-continuity-qa | T-016, T-020, T-024 | One QA-ready matrix covers preserve, clear, restore, and helper-copy behavior across desktop, tablet, and mobile workspace flows, with references back to the locked workspace contract and session-state examples | Frontend and QA can validate deterministic selection continuity without inferring missing cases, and each scenario maps to expected `selectedJobId`, `lastSelectedJobId`, and helper-state outcomes | Claimed during hourly kickoff after Jimmy asked Priya to lock deterministic selection continuity across filters, sorting, dashboard return flows, and mobile/tablet behavior |
| T-029 | Deliver the first authenticated persisted-jobs vertical slice by wiring session-backed Gmail search results into the jobs UI | Alice | in-progress | feat/jobtrackr-auth-session-gmail-connect | T-018, T-022, T-026 | The authenticated app can load persisted jobs after Gmail connection, reuse the session flow, and render deterministic server-backed results in the jobs UI without falling back to mock-only behavior | After Google auth and Gmail readonly connect, the frontend can request persisted jobs through the signed-in session, show loading and empty states from real API responses, and document the handoff point between auth/session work and persisted search filtering | Claimed during hourly kickoff after Jimmy directed Alice to complete the first real auth -> session -> Gmail connection -> persisted job retrieval vertical slice |
| T-030 | Harden the job details editing UI with contract-aligned save/cancel notes flow, in-context tag edits, and shared section structure across drawer and page views | Marcus | in-progress | feat/jobtrackr-job-details-editing | T-012, T-019, T-023, T-027 | The mock details UI matches the canonical section order, notes require explicit save/cancel, tags edit directly in context, and the component is ready for later API wiring without interaction ambiguity | The jobs drawer and full-page detail view share the same header, fit/workflow, content, notes, and tags structure, notes do not autosave, tag edits are obvious, and the component behavior matches the canonical detail-view examples | Claimed during hourly kickoff to convert the locked detail-view contract into implementation-ready UI behavior on Marcus's active branch |
| T-031 | Implement deterministic row-selection continuity in the mock jobs workspace across filter changes, pagination, and dashboard return flows | Priya | in-progress | feat/jobtrackr-row-selection-model | T-013, T-016, T-020, T-024, T-028 | Jobs workspace preserves, clears, and restores selection predictably with helper copy and responsive affordances when the selected row leaves or re-enters the visible result set | The mock frontend keeps one deterministic `selectedJobId` and `lastSelectedJobId` model across jobs filters, page changes, and dashboard return links, with explicit recovery UI when a selection is out of view or filtered out | Claimed during hourly kickoff after Jimmy asked Priya to harden the deterministic selection model before frontend behavior drifts |
| T-032 | Rewrite stale Gate A docs so legacy workflow statuses and auto-close behavior no longer conflict with the canonical PM memo, API contract, and reconciliation matrix | Frank | in-progress | chore/jobtrackr-gate-a-stale-doc-cleanup | T-017, T-025 | The stale auto-close and MVP story docs are rewritten or clearly marked so engineering does not reuse superseded workflow/archive semantics during implementation pickup | Engineers reading older follow-through docs no longer see `flagged`, `reviewing`, `skipped`, `interview`, or `not a match` presented as canonical workflow states, and the cleanup points back to the current Gate A source-of-truth set | Claimed during hourly kickoff after Jimmy's latest watchouts highlighted lingering stale workflow semantics in older follow-through docs |
| T-033 | Reconcile DEVELOPMENT_PLAN ticket language with the canonical workflow, fit, and table-contract decisions so engineers stop inheriting stale implementation assumptions | Alice | in-progress | chore/jobtrackr-development-plan-reconciliation | T-001, T-004, T-017, T-025 | DEVELOPMENT_PLAN no longer presents superseded workflow statuses, auto-close semantics, or stale flagged behavior as implementation truth, and any still-open ticket copy clearly points back to the canonical source docs | Engineers can pick up tasks from DEVELOPMENT_PLAN without reintroducing `not a match` as a workflow status, mistaking fit indicators for workflow state, or following stale milestone wording that conflicts with Gate A decisions | Claimed during hourly kickoff after Jimmy asked for the development plan ticket language to be rewritten and reconciled |
| T-034 | Audit and reconcile milestone and ticket wording in DEVELOPMENT_PLAN so canonical workflow, fit, and table semantics stay consistent after the ticket rewrite pass | Marcus | in-progress | chore/jobtrackr-development-plan-reconciliation | T-001, T-004, T-017, T-025, T-033 | Milestone steps and active-ticket wording use the same canonical workflow and fit language, with stale assumptions removed or redirected to source-of-truth docs | Engineers reading either the task table or milestone breakdown see one consistent contract for workflow statuses, low-fit handling, and table payload semantics without reintroducing legacy terms | Claimed during hourly kickoff after Jimmy asked for the development plan rewrite to be reconciled all the way through the engineering plan |
| T-045 | Reconcile PROJECT, milestone, and handoff references so the plan rewrite stays aligned outside the task table | Marcus | in-progress | chore/jobtrackr-development-plan-reconciliation | T-034 | PROJECT.md, milestone wording, and handoff-facing references use the same canonical workflow and fit language as the reconciled development plan, with stale assumptions removed or redirected to the source-of-truth docs | Engineers can move between DEVELOPMENT_PLAN, PROJECT.md, and the implementation milestone docs without seeing `flagged`, `reviewing`, `skipped`, `interview`, or `not a match` presented as current workflow truth, and fit signals remain clearly separate from workflow state | Claimed during hourly kickoff after Jimmy fetch was blocked and no unclaimed Marcus task remained in the active table, so Marcus pulled the next reconciliation follow-through on the active development-plan branch |
| T-046 | Reconcile PRD and QA test-case references so legacy workflow and auto-close language no longer conflicts with the canonical workflow and fit model | Priya | in-progress | chore/jobtrackr-development-plan-reconciliation | T-033, T-034, T-045 | PRD and in-progress QA test cases stop presenting `flagged`, `reviewing`, `skipped`, `interview`, and `not a match` as canonical workflow or auto-close outcomes, while still preserving the intended fit-signal and low-fit coverage | Engineers and QA can read the PRD plus active test cases without inheriting stale status enums or auto-close behavior, and all low-fit coverage points back to the canonical fit-signal contract instead of a workflow mutation | Claimed during hourly kickoff after Jimmy fetch was blocked and no unclaimed active follow-through remained, so Priya pulled the next reconciliation task on the active development-plan branch |
| T-047 | Reconcile remaining product/spec references so legacy flagged and stale workflow language no longer conflicts with the canonical workflow and fit-signal model | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-032, T-033, T-034, T-045, T-046 | Remaining product and implementation specs that still use legacy workflow or flagged wording are rewritten or annotated to align with the canonical workflow statuses and fit-signal semantics | Engineers can read the data model, table UI, fit-analysis, and first-slice specs without mistaking fit indicators for workflow state or inheriting stale status enums | Claimed during hourly kickoff after Jimmy fetch was blocked and no unclaimed active follow-through remained, so Frank pulled the next spec-reconciliation task on the active development-plan branch |
| T-048 | Reconcile parser and ingestion ticket wording so milestone follow-through docs stop implying deprecated auto-close or workflow-state behavior for low-fit jobs | Alice | in-progress | chore/jobtrackr-development-plan-reconciliation | T-033, T-034, T-047 | Parser and ingestion breakdown docs use the canonical low-fit and fit-signal language, keep workflow state separate from parser responsibilities, and point implementation back to the locked source-of-truth set where needed | Engineers can pick up Milestone 4 parser work without inferring `not a match`, parser-driven workflow mutation, or stale flagged semantics from follow-through ticket wording | Claimed during hourly kickoff after Jimmy fetch was blocked and no unclaimed Alice task remained, so Alice pulled the next milestone follow-through reconciliation task on the active development-plan branch |
| T-049 | Remove legacy workflow alias handling from the persisted jobs UI mapping and surface canonical fit-signal defaults for server-backed job results | Alice | in-progress | feat/jobtrackr-auth-session-gmail-connect | T-029, T-033, T-048 | Persisted jobs UI maps backend results without reintroducing legacy workflow statuses, derives low-fit from canonical fit signals only, and documents safe fallback behavior while fit analysis is not yet present in the API | Frontend only accepts canonical workflow statuses from the jobs API, `flagged` and `not a match` are no longer treated as workflow aliases, and server-backed jobs render deterministic match and low-fit defaults without implying a stale workflow transition | Claimed during hourly kickoff after Jimmy fetch was blocked and no unclaimed Alice task remained, so Alice pulled the next implementation follow-through directly from the persisted jobs vertical slice |
| T-050 | Reconcile PRD v2 dashboard, status, and fit-language sections plus in-progress QA coverage so archived stays out of workflow state and low-fit remains a fit signal | Priya | in-progress | chore/jobtrackr-development-plan-reconciliation | T-046 | PRD v2 and active QA cases consistently describe archived as view/archive state only, keep workflow statuses canonical, and frame low-fit behavior as fit-signal handling instead of workflow mutation | Engineers and QA can read the current PRD plus in-progress test cases without inheriting `archived` as a status, fake auto-close outcomes, or missing match-rating / low-fit signal guidance in dashboard coverage | Claimed during hourly kickoff after Jimmy fetch was blocked and no unclaimed active task remained, so Priya pulled the next PRD-and-QA reconciliation pass on the active development-plan branch |
| T-051 | Reconcile remaining fit-signal wording in API, frontend, and clarification docs so `flagged` and `unflagged` stop reading like canonical product states | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-047 | Remaining docs use canonical fit-signal wording such as low-fit, strong-fit, fit-ready, and pending-fit without presenting `flagged` or `unflagged` as product-state truth, and examples stay aligned with the Gate A source set | Engineers can read the API filters, frontend state definitions, and clarification notes without inheriting `flagged` or `unflagged` as canonical user-facing state names, while still preserving deterministic filtering examples and nullable-fit semantics | Claimed during hourly kickoff after Jimmy fetch was blocked and the remaining open cleanup was concentrated in API, frontend, and clarification docs on the active development-plan branch |
| T-052 | Harden the persisted jobs frontend mapping so server-backed results stay canonical when fit analysis is missing or legacy workflow aliases leak through old data | Alice | in-progress | feat/jobtrackr-auth-session-gmail-connect | T-029, T-049 | Persisted jobs UI treats unknown workflow values as safe canonical defaults, removes legacy alias inference, preserves pending-fit behavior when match data is absent, and documents the fallback contract for the current backend slice | Frontend does not infer match rating from freeform text, `flagged` and `not a match` do not appear as accepted workflow aliases, and users see deterministic pending-fit behavior until the API ships real fit-analysis fields | Claimed during hourly kickoff after Jimmy fetch returned 401 and no unclaimed Alice task remained, so Alice pulled the next persisted-jobs hardening pass directly on the active auth/session/Gmail branch |
| T-053 | Reconcile remaining legacy workflow and fit-signal wording in older stories and superseded PRD/spec drafts so stale docs stop leaking non-canonical states back into implementation | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-047, T-051 | Legacy story files plus superseded PRD/spec drafts are rewritten or clearly annotated to defer to the canonical workflow and fit-signal docs, with stale status and auto-close wording removed where practical | Engineers skimming older story or draft-doc context no longer inherit `flagged`, `unflagged`, `not a match`, `reviewing`, or `skipped` as current product truth, and each preserved historical file clearly points back to the canonical source set | Claimed during hourly kickoff after Jimmy fetch returned 401 and no unclaimed active task remained, so Frank pulled the remaining stale-story and draft-doc reconciliation pass on the active development-plan branch |
| T-054 | Reconcile entrypoint and clarification docs so historical context keeps pointing engineers back to the canonical workflow and fit-signal vocabulary | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-053 | PROJECT, clarification notes, and preserved draft-entrypoint docs use the same canonical language and explicit source-of-truth redirects, without leaving `flagged` or `unflagged` as implied product-state names | Engineers opening high-level project entrypoints or retained clarification docs immediately see the current source-of-truth set, and any historical references to fit flags are framed as legacy shorthand instead of live product semantics | Claimed during hourly kickoff after Jimmy fetch remained blocked and no unclaimed active task remained, so Frank pulled the next entrypoint-doc reconciliation pass on the active development-plan branch |
| T-055 | Surface canonical persisted-jobs fallback reasons in the frontend so missing fit data and normalized workflow values are visible per result set without implying stale workflow aliases | Alice | in-progress | feat/jobtrackr-auth-session-gmail-connect | T-029, T-049, T-052 | Persisted jobs UI exposes which rows are using pending-fit fallback or workflow canonicalization, keeps the messaging tied to canonical fit signals, and gives the jobs workspace and dashboard enough detail to explain the current backend slice without reviving legacy status names | Users reviewing server-backed jobs can see when a result is still pending fit or was normalized to a canonical workflow state, while the UI continues rejecting legacy aliases like `flagged` and `not a match` as product-state truth | Claimed during hourly kickoff after Jimmy fetch returned 401 and no unclaimed Alice task remained, so Alice pulled the next persisted-jobs visibility follow-through on the active auth/session/Gmail branch |
| T-056 | Reconcile kickoff entrypoint docs so README and first-build-slice guidance point engineers at the canonical auth/dashboard vertical slice and current project phase | Marcus | in-progress | chore/jobtrackr-development-plan-reconciliation | T-034, T-045 | README and kickoff guidance reflect the active engineering-handoff phase, point to the canonical source-of-truth docs, and describe the first build slice without stale or ambiguous fit/workflow wording | An engineer opening README or the first-build-slice doc sees the current project phase, the canonical kickoff slice, and explicit source-of-truth references without relying on outdated discovery-era framing | Claimed during hourly kickoff after Jimmy fetch returned 401 and no unclaimed Marcus task remained, so Marcus pulled the next kickoff-entrypoint reconciliation task on the active development-plan branch |
| T-057 | Reconcile PRD v2 and QA blocker checklist source-of-truth references so archive and fit-signal guidance stay canonical in implementation-facing QA docs | Priya | in-progress | chore/jobtrackr-development-plan-reconciliation | T-046, T-050 | PRD v2 and the QA blocker checklist agree on archive-state, workflow-status, and fit-signal language, and their source-of-truth references point to the current project paths | Engineers and QA can open the PRD and shortest blocker checklist without hitting stale archive questions, bad source paths, or wording that implies low-fit or archived are workflow states | Claimed during hourly kickoff after Jimmy fetch returned 401 and no unclaimed active task remained, so Priya pulled the next implementation-facing PRD and QA checklist reconciliation pass on the active development-plan branch |
| T-058 | Reconcile roadmap fit-language and release exit criteria so release sequencing keeps using canonical fit signals instead of legacy flagged wording | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-047, T-051, T-054 | ROADMAP.md uses canonical fit-signal language across release scope, outcomes, and exit criteria, and release sequencing still matches the active engineering handoff direction | Engineers and PM can read roadmap release goals without inheriting `flagged` as product-state vocabulary, while match percentage, low-fit handling, and fit rationale remain clearly separated from workflow state | Claimed during hourly kickoff after Jimmy fetch remained blocked and no unclaimed active task remained, so Frank pulled the roadmap reconciliation follow-through on the active development-plan branch |
| T-059 | Add a protected local session shell and explicit Gmail disconnect flow for the Gmail job search vertical slice | Alice | in-progress | feat/gmail-job-search-webapp | T-002, T-018, T-022 | The local Go app requires an authenticated app session before exposing Gmail search results, supports a clean Gmail disconnect path, and documents the local auth contract for the MVP slice | A user must unlock the local shell before viewing inbox-derived jobs, can disconnect Gmail without deleting local app state manually, and the implementation avoids hardcoded OAuth state or always-open inbox access | Claimed during hourly kickoff after Jimmy fetch returned 401 and no unclaimed Alice task remained, so Alice pulled the next auth-shell follow-through on the active Gmail webapp branch |
| T-060 | Reconcile lingering legacy fit-flag shorthand in preserved story drafts and clarification docs so historical references stop leaking stale product vocabulary | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-053, T-054, T-058 | Preserved historical story drafts and clarification docs either use canonical fit-signal language or are explicitly annotated so engineers cannot misread `flagged` or `unflagged` as current product-state vocabulary | Engineers skimming preserved story drafts and clarification notes do not inherit legacy fit-flag shorthand as live product terminology, and every retained historical reference points back to the PM memo, API contract, and DEVELOPMENT_PLAN as the source of truth | Claimed during hourly kickoff after Jimmy's latest plan emphasized closing remaining source-of-truth drift in preserved docs |
| T-061 | Reconcile README, PROJECT, and kickoff handoff entrypoints so engineers land on the current auth to Gmail to persisted-jobs slice with correct project-root paths | Marcus | in-progress | chore/jobtrackr-development-plan-reconciliation | T-045, T-056 | README, PROJECT, first-build-slice, and handoff entrypoints agree on the current implementation slice, phase wording, and root-relative source-of-truth paths without stale mock-only kickoff framing | An engineer opening any kickoff doc sees the current auth to Gmail to persisted-jobs direction, follows valid `projects/jobtrackr/...` paths from shared handoff docs, and is not redirected into outdated mock-only sequencing | Claimed during hourly kickoff after Jimmy's latest plan emphasized entrypoint safety and no unclaimed Marcus task remained in the active table, so Marcus pulled the next kickoff-doc alignment follow-through on the active reconciliation branch |
| T-062 | Reconcile QA-owned source-path and environment notes so implementation-facing test guidance points at the live project root and current web-plus-API architecture | Priya | in-progress | chore/jobtrackr-development-plan-reconciliation | T-057 | QA blocker checklist and in-progress test coverage point to valid `projects/jobtrackr/...` paths, clearly distinguish the current Next.js web plus Go API slice from older local-Go-app wording, and keep the shortest QA entrypoints safe to follow during implementation | QA can open the checklist and active test coverage without hitting dead root paths, stale environment assumptions, or ambiguity about which docs govern the current auth -> Gmail -> persisted-jobs slice | Claimed during hourly kickoff after the latest prompt still referenced a dead `~/Documents/project-requirements/DEVELOPMENT_PLAN.md` path and QA noted environment drift between older local-Go-app wording and the current web-plus-API handoff docs |
| T-063 | Reconcile roadmap and legacy review-doc source references so preserved planning docs point back to the current auth -> Gmail -> persisted-jobs source-of-truth set | Frank | in-progress | chore/jobtrackr-development-plan-reconciliation | T-054, T-058, T-060 | ROADMAP plus older PM and QA review docs use valid `projects/jobtrackr/...` references, frame the current implementation slice explicitly, and avoid legacy fit-flag wording as live product vocabulary | Engineers opening roadmap, PM memo, or legacy QA/spec-review docs land on the current source-of-truth files, see the active auth -> Gmail -> persisted-jobs slice called out directly, and do not inherit `flagged` or dead-path references as canonical guidance | Claimed during hourly kickoff after Jimmy's latest plan emphasized roadmap cleanup, preserved historical-doc safety, and explicit source-of-truth redirects |
| T-064 | Reconcile automation-facing kickoff references so hourly Alice and QA prompts stop pointing at the dead root-level development-plan path and instead call out the live project-root source-of-truth set | Alice | in-progress | chore/jobtrackr-development-plan-reconciliation | T-057, T-062 | DEVELOPMENT_PLAN, QA notes, and preserved kickoff guidance explicitly document the live `projects/jobtrackr/...` source paths plus the current auth -> session -> Gmail readonly connect -> persisted jobs implementation slice for automation-facing handoffs | Engineers following hourly kickoff prompts or the shortest QA entrypoints can recover to the correct project-root docs without guessing, and the dead `~/Documents/project-requirements/DEVELOPMENT_PLAN.md` path is called out as historical drift rather than an active pickup path | Claimed during hourly kickoff after Jimmy's latest plan and the current cron prompt still referenced the dead root-level development-plan path, so Alice pulled the automation-facing source-reference cleanup follow-through on the active reconciliation branch |
| T-065 | Reconcile historical kickoff slice guidance so preserved first-build docs explicitly recover to the live project-root source files and current auth -> session -> Gmail readonly connect -> persisted jobs lane | Marcus | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-056, T-061, T-064 | Historical kickoff docs clearly label the mock-first slice as preserved context, point engineers back to the live `projects/jobtrackr/...` source set, and explain how to recover when automation-facing prompts or older notes reference stale paths or outdated slice framing | Engineers opening preserved kickoff docs can immediately recover to the current implementation lane, valid project-root paths, and canonical source files without guessing whether mock-only guidance is still active | Claimed during hourly kickoff after Jimmy's latest plan kept Marcus on kickoff-entrypoint alignment and the preserved first-build slice still needed an explicit recovery note for stale automation prompts and project-root paths |
| T-066 | Reconcile remaining QA recovery references in PRD v2 and in-progress test coverage so QA can recover from stale automation prompts without guessing the live project-root entrypoints or current web-plus-API slice | Priya | in-progress | chore/jobtrackr-development-plan-reconciliation | T-062 | PRD v2, QA blocker checklist, and in-progress QA coverage explicitly recover to `projects/jobtrackr/...` source-of-truth files, frame the current implementation as Next.js web plus Go API, and label root-level kickoff path drift as external coordination rather than repo ambiguity | QA can open the shortest implementation-facing docs, recover from stale `~/Documents/project-requirements/DEVELOPMENT_PLAN.md` prompts, and understand the active auth -> session -> Gmail readonly connect -> persisted jobs slice without inheriting older local-Go-app assumptions | Claimed during hourly kickoff after Jimmy's latest plan kept Priya on QA path and environment alignment, and T-062 exposed remaining recovery-note drift in PRD v2 plus in-progress QA coverage |
| T-067 | Reconcile roadmap and legacy review-doc kickoff notes so planning artifacts explicitly recover to the live project-root source files and current Next.js web plus Go API delivery slice | Frank | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-063 | ROADMAP plus preserved PM and QA review docs point to `projects/jobtrackr/...`, describe the active implementation as Next.js web plus Go API, and explain how to recover when older automation prompts still mention the dead root-level development-plan path | Engineers opening planning or review artifacts can recover immediately to the live kickoff set, understand the auth -> session -> Gmail readonly connect -> persisted jobs lane, and avoid mistaking older local-Go-app framing for the current architecture | Claimed during hourly kickoff after Jimmy's latest plan kept Frank on source-of-truth cleanup across roadmap and preserved review docs |
| T-068 | Normalize kickoff entrypoint path examples so README, PROJECT, and the Phase 3 handoff package all point to the same explicit `projects/jobtrackr/...` source-of-truth files during hourly recovery | Marcus | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-061, T-065 | Kickoff-facing entrypoint docs use explicit project-root paths in their ordered source-of-truth lists, preserve the current auth -> session -> Gmail readonly connect -> persisted jobs pickup lane, and remove ambiguity between repo-root filenames and project-root handoff references | Engineers recovering from hourly prompts or preserved kickoff notes can open README, PROJECT, or the handoff package and see the same explicit `projects/jobtrackr/...` entrypoints without guessing whether bare filenames refer to the live project root or stale root-level docs | Claimed during hourly kickoff after Jimmy's latest reminder kept the live entrypoints on `projects/jobtrackr/PROJECT.md` and `projects/jobtrackr/DEVELOPMENT_PLAN.md`, while kickoff-facing docs still mixed explicit project-root paths with bare filenames |
| T-069 | Normalize automation-facing recovery examples so hourly kickoff prompts can recover from the stale root-level development-plan path without ambiguity about the current project-root source set or live web-plus-API slice | Alice | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-064, T-068 | Automation-facing kickoff docs and preserved recovery notes use the same explicit `projects/jobtrackr/...` examples, point to the live engineering handoff files in the same order, and reinforce that the current implementation is the Next.js web plus Go API slice | Engineers following an hourly prompt can recover from `~/Documents/project-requirements/DEVELOPMENT_PLAN.md` to the live project-root docs immediately, without guessing whether to start from README, PROJECT, or the handoff package, and without inheriting older local-Go-app wording | Claimed during hourly kickoff after Jimmy's latest plan kept Alice on automation-facing recovery and no unclaimed task remained in the active table, so Alice pulled the next kickoff-example normalization pass on the active kickoff-recovery branch |
| T-070 | Align QA recovery wording across PRD v2, the QA blocker checklist, and in-progress QA coverage so all three use the same explicit project-root entrypoints and current Next.js web plus Go API framing | Priya | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-066 | PRD v2, the shortest QA blocker checklist, and in-progress QA coverage share one recovery pattern for stale automation prompts, point to the same `projects/jobtrackr/...` source-of-truth files, and describe the active implementation slice with the same architecture wording | QA can move between PRD, blocker checklist, and active test coverage without path drift, mixed recovery instructions, or older local-Go-app wording being mistaken for the current default implementation context | Claimed during hourly kickoff after Jimmy's latest plan kept Priya on QA recovery alignment and no unclaimed active task remained, so Priya pulled the next QA wording-normalization follow-through on the active kickoff-recovery branch |
| T-071 | Reconcile preserved engineering handoff guidance so legacy kickoff docs recover to the live project-root source files and current Next.js web plus Go API delivery slice | Frank | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-067 | Preserved handoff docs explicitly point to `projects/jobtrackr/...`, label historical build-order guidance as legacy context, and redirect pickup to the current auth -> session -> Gmail readonly connect -> persisted jobs lane | Engineers opening older handoff guidance can recover to the live source-of-truth files immediately, avoid bare repo-root path ambiguity, and do not mistake older TypeScript-only backend guidance for the current Next.js web plus Go API implementation slice | Claimed during hourly kickoff after Jimmy's latest plan kept Frank on kickoff recovery cleanup and no unclaimed Frank task remained in the active table, so Frank pulled the next preserved-handoff reconciliation pass on the active kickoff-recovery branch |
| T-072 | Normalize kickoff-facing path examples and lane wording so README, PROJECT, and the handoff package use the same explicit recovery order and active implementation language | Marcus | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-061, T-065, T-068 | Kickoff-facing entrypoint docs share one explicit project-root recovery sequence, use the same Next.js web plus Go API lane wording, and remove remaining bare-path ambiguity from repo structure and pickup guidance | Engineers recovering from hourly prompts can move between README, PROJECT, and the handoff package without guessing whether a bare filename is live, historical, or shorthand for the project-root source set | Claimed during hourly kickoff after Jimmy's latest plan kept Marcus on kickoff-entrypoint normalization and no unclaimed Marcus task remained in the active table, so Marcus pulled the next cross-entrypoint cleanup pass on the active kickoff-recovery branch |
| T-073 | Normalize preserved engineering handoff recovery wording so older handoff docs use the same explicit project-root entrypoints, legacy-context labeling, and current Next.js web plus Go API pickup language | Frank | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-071 | Preserved handoff docs share the same recovery order, explicitly label mock-first and older build-order notes as historical context, and steer engineers back to the live auth -> session -> Gmail readonly connect -> persisted jobs lane without bare-path ambiguity | Engineers opening older handoff docs can recover to `projects/jobtrackr/DEVELOPMENT_PLAN.md`, `projects/jobtrackr/PROJECT.md`, and the current Phase 3 handoff package in a consistent order, while seeing the preserved mock-first guidance framed as historical rather than the active implementation default | Claimed during hourly kickoff after Jimmy's latest plan kept Frank on preserved handoff cleanup and no unclaimed active Frank task remained, so Frank pulled the next handoff-wording normalization follow-through on the active kickoff-recovery branch |
| T-074 | Normalize preserved implementation-facing recovery notes so older handoff and QA review docs use the same explicit absolute-path recovery order and current Next.js web plus Go API lane wording | Alice | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-069 | Preserved implementation-facing docs share one absolute-path recovery pattern for stale hourly prompts, point to the same live project-root entrypoints, and describe the active delivery slice with the same architecture wording | Engineers opening preserved handoff or QA review docs can recover from `~/Documents/project-requirements/DEVELOPMENT_PLAN.md` to the live `projects/jobtrackr/...` files without doc-specific variations, and they see the current Next.js web plus Go API lane described the same way in each preserved entrypoint | Claimed during hourly kickoff after Jimmy's latest plan kept Alice on automation-facing recovery cleanup and the preserved handoff plus QA review docs still used slightly different stale-path recovery wording |
| T-075 | Normalize the shared QA recovery note across PRD v2, the blocker checklist, and in-progress QA coverage so the path-recovery order and current implementation lane wording are byte-for-byte aligned | Priya | completed | chore/jobtrackr-kickoff-recovery-docs | T-070 | PRD v2, the QA blocker checklist, and in-progress QA coverage reuse one identical recovery note that names the same stale path, ordered project-root recovery files, and current Next.js web plus Go API delivery slice | QA can compare the three primary implementation-facing docs and find the same recovery text without path-order drift, missing `session` wording, or doc-specific shorthand that could reintroduce confusion during hourly kickoff recovery | Completed during the 6:50 PM hourly kickoff after Priya byte-aligned the shared QA recovery note header and wording across `prd/jobtrackr-prd-v2.md`, `specs/jobtrackr-qa-blocker-checklist-2026-04-19.md`, and `stories/jobtrackr-in-progress-test-cases-2026-04-19.md`, then re-verified the stale-path recovery order and active Next.js web plus Go API lane wording matched exactly. |
| T-076 | Normalize preserved planning and handoff recovery notes so ROADMAP, the PM memo, QA testability review, and legacy handoff docs share the same explicit recovery order and active implementation lane wording | Frank | completed | chore/jobtrackr-preserved-handoff-recovery-alignment | T-063, T-073 | Preserved planning and handoff docs reuse one consistent stale-path recovery note, point to the same ordered `projects/jobtrackr/...` kickoff set, and describe the active Next.js web plus Go API slice with the same wording | Engineers skimming ROADMAP, the PM memo, QA review notes, or preserved handoff docs can recover from stale kickoff prompts without doc-specific path drift, lane-wording drift, or ambiguity about which files are live | Completed during the 8:10 PM hourly kickoff after Frank aligned the preserved QA review, engineering handoff v1, first-build slice, and Phase 3 handoff package to the same recovery order and current lane wording already used by ROADMAP and the PM decision memo. |
| T-078 | Normalize kickoff-facing lane ownership so README, PROJECT, and the Phase 3 handoff package explicitly reflect Jimmy's frontend split for Marcus and Alice while keeping the live project-root recovery order intact | Marcus | completed | chore/jobtrackr-kickoff-recovery-docs | — | Kickoff-facing entrypoints describe Marcus and Alice as frontend owners, keep the current Next.js web plus Go API slice wording, and preserve one explicit recovery order for stale automation prompts | Engineers opening README, PROJECT, or the handoff package can see the current frontend ownership split immediately, recover from stale root-level path prompts without ambiguity, and still land on the same live source-of-truth files | Completed during the 8:40 PM hourly kickoff after Marcus aligned README, PROJECT, and the Phase 3 handoff package to the same frontend-ownership split, explicitly called out Alice as primary frontend delivery and Marcus as frontend continuity owner, and preserved the same live project-root recovery order across all three kickoff-facing entrypoints. |
| T-079 | Normalize roadmap-facing lane ownership so ROADMAP and preserved planning guidance explicitly reflect Jimmy's frontend split for Alice and Marcus while keeping Frank on source-of-truth maintenance | Frank | in-progress | chore/jobtrackr-kickoff-recovery-docs | T-076, T-078 | Roadmap-facing planning docs describe Alice and Marcus as frontend owners for the current slice, keep backend and shared-contract follow-through assigned to the remaining SWE lanes, and preserve the live project-root recovery order | Engineers reading ROADMAP or preserved planning guidance can see the current frontend/backend split immediately, recover from stale kickoff prompts without ambiguity, and follow the same active Next.js web plus Go API delivery slice wording used in kickoff-facing docs | Claimed during hourly kickoff after Jimmy's latest plan redirected Alice and Marcus to frontend work and no unclaimed Frank task remained in the active table |
| T-080 | Normalize preserved implementation-facing recovery and lane-ownership wording so the historical first-build slice, engineering handoff v1, and QA testability review all reflect the same frontend split for Marcus and Alice and the same explicit project-root recovery order | Marcus | completed | chore/jobtrackr-kickoff-recovery-docs | T-065, T-072, T-078 | The preserved implementation-facing docs reuse one identical recovery pattern, explicitly describe Marcus and Alice as frontend owners for the current slice, and keep the active Next.js web plus Go API lane wording aligned with kickoff-facing entrypoints | Engineers opening preserved implementation-facing docs can recover from stale root-level prompts using the same ordered project-root files, see the same current frontend ownership split, and avoid doc-specific drift in lane wording or recovery steps | Completed during hourly kickoff after Marcus rewrote `specs/jobtrackr-first-build-slice-v1.md`, `specs/jobtrackr-engineering-handoff-v1.md`, and `specs/jobtrackr-mvp-qa-testability-review.md` to share the same preserved-doc recovery block, frontend split, and current Next.js web plus Go API lane wording. |
| T-081 | Byte-align the preserved implementation-facing frontend-ownership note so the first-build slice, engineering handoff v1, and QA testability review all reuse the same wording for Marcus and Alice's current lane split | Marcus | completed | chore/jobtrackr-kickoff-recovery-docs | T-080 | The three preserved implementation-facing docs use the same frontend-ownership sentence and matching current-lane wording, without doc-specific qualifiers that could reintroduce pickup ambiguity during hourly recovery | Engineers opening any preserved implementation-facing doc see the same Marcus-and-Alice frontend ownership wording, the same remaining-lanes backend/shared-contract wording, and no variation that implies different pickup rules by document | Completed during hourly kickoff after Marcus byte-aligned the shared frontend ownership note to `Alice and Marcus are the frontend owners for the current slice.` across the preserved first-build slice, engineering handoff v1, and QA testability review. |
| T-083 | Byte-align roadmap-facing lane-ownership wording so ROADMAP and the PM decision memo use the same current frontend split for Alice and Marcus while keeping Frank on source-of-truth maintenance | Frank | completed | chore/jobtrackr-preserved-handoff-recovery-alignment | T-079 | ROADMAP and the PM decision memo reuse one identical lane-ownership note for the current slice, preserve the same project-root recovery order, and keep Frank explicitly on source-of-truth maintenance while Alice and Marcus stay frontend-facing | Engineers opening roadmap-facing planning docs can see the same current ownership split immediately, recover from stale kickoff prompts without wording drift, and avoid conflicting interpretations of Frank's lane versus the frontend pickup lanes | Completed during the 8:10 PM hourly kickoff after Frank re-verified ROADMAP and the PM decision memo were byte-aligned on lane ownership, then updated the preserved handoff docs to match that same roadmap-facing wording. |
| T-085 | Byte-align kickoff-facing recovery order and lane wording so README, PROJECT, and the Phase 3 handoff package all use the same explicit project-root sequence and current frontend split | Marcus | completed | chore/jobtrackr-kickoff-recovery-docs | T-072, T-078, T-081 | README, PROJECT, and the Phase 3 handoff package reuse one explicit `projects/jobtrackr/...` recovery order, the same current Next.js web plus Go API lane wording, and the same frontend-ownership note for Alice and Marcus | Engineers opening any kickoff-facing entrypoint can recover from stale hourly prompts without path-order drift, and they see the same current Alice-and-Marcus frontend ownership wording in every live kickoff doc Marcus owns | Completed during hourly kickoff after Marcus found the remaining drift in the Phase 3 handoff package's Alice lane guidance, rewrote it to match the current frontend ownership split, and re-verified the shared recovery order stayed aligned with README and PROJECT. |
| T-103 | Reconcile preserved frontend detail-flow specs so the frontend plan and table UI spec stop drifting from the canonical detail-view and list-to-detail contracts | Frank | completed | chore/jobtrackr-detail-continuity-alignment | T-015, T-023, T-027, T-030 | The preserved frontend plan and table UI spec explicitly defer to the canonical detail-view and list-to-detail docs, remove stale detail-flow wording, and describe notes, tags, and selection continuity using the current shared contract | Engineers reading preserved frontend planning docs see one current detail interaction model for drawer and full-page views, do not inherit outdated `strengths/gaps/notes later` wording, and can trace table-to-detail continuity back to the canonical contract files | Claimed and completed during the 6:10 PM hourly kickoff after Jimmy's latest plan emphasized keeping detail-view and frontend continuity references coherent with the canonical list/detail contract. |
| T-104 | Refresh in-progress QA coverage after Jimmy's latest frontend-split update so active verification notes reflect T-103 completion, the preserved-doc regression state, and the remaining T-095 sign-off gap | Alice | completed | chore/jobtrackr-detail-continuity-alignment | T-086, T-102, T-103 | The active QA coverage file records Jimmy's 6:02 PM frontend-owner guidance, confirms the preserved-doc regression surface remains aligned after T-103, and keeps the remaining T-095 fixture-backed sign-off gap explicit | A reviewer can open the in-progress QA coverage file and immediately see the latest Jimmy fetch takeaways, the newly completed preserved-doc tasks, the current stale external-path coordination gap, and the exact remaining QA proof needed for T-095 | Claimed and completed during the 6:30 PM hourly kickoff after the live plan had no unclaimed active slot, so Alice pulled the smallest safe finishable repo-side verification task on the current detail-continuity branch. |
| T-105 | Add kickoff-facing frontend continuity pointers so README, PROJECT, and the Phase 3 handoff package explicitly route frontend pickup back to the canonical detail-view, list-to-detail, and workspace continuity contracts | Marcus | completed | chore/jobtrackr-detail-continuity-alignment | T-103 | Kickoff-facing docs call out the canonical detail continuity files alongside the current frontend ownership split, and preserved frontend specs remain clearly secondary to the live contract set | An engineer opening README, PROJECT, or the handoff package can recover from hourly kickoff directly into the current detail-view, list-to-detail, and workspace continuity docs without relying on older preserved frontend-plan wording | Claimed and completed during the 6:40 PM hourly kickoff after Jimmy's latest plan kept Marcus on frontend continuity alignment and the active table had no unclaimed Marcus slot. |
| T-106 | Wire the dashboard and jobs workspace to the persisted-jobs API with safe mock fallback visibility so Alice's frontend slice stops depending on mock-only data when the backend is available | Sam | QA | feat/jobtrackr-persisted-jobs-workspace | T-018, T-022, T-026, T-029, T-055 | Dashboard and jobs workspace both load persisted jobs through the shared frontend data layer, preserve mock fallback when the API is unavailable, surface fallback reasons when canonicalization or pending-fit defaults are in play, and include tests/docs for the mapping contract | With the API running, the dashboard and jobs workspace render server-backed jobs plus fallback notices for pending-fit and workflow normalization; without the API, the same screens safely fall back to mock data without breaking navigation or selection continuity | Claimed during the 9:30 PM hourly kickoff after Jimmy's latest plan kept Alice on the auth/session/Gmail/persisted-jobs frontend slice and the active table had no genuinely unclaimed Alice ticket left, so Alice pulled the smallest safe finishable frontend follow-through on a fresh branch. Implementation completed the same hour by wiring `/dashboard` and `/jobs` to `getJobsResult`, surfacing persisted-job fallback notices plus source labels, preserving job-table page context on row selection, adding API-mapping coverage in `apps/web/lib/jobApiMapping.test.ts`, updating `apps/web/README.md`, and re-running `npm test` plus `npm run build` in `apps/web` before handing the ticket to Sam for QA. |
| T-107 | Refresh QA hourly coverage after Jimmy's latest plan so Priya's repo-facing validation notes reflect the live frontend slice handoff and current remaining QA blockers | Priya | completed | chore/jobtrackr-qa-hourly-coverage-refresh | T-086, T-095, T-106 | The in-progress QA coverage file captures Jimmy's latest plan, records T-106 as the new frontend QA handoff, and keeps the remaining blocker story narrowed to T-095 fixture-backed validation plus stale external kickoff drift | A reviewer can open the active QA coverage file and immediately see the current frontend pickup state, the new server-backed workspace QA target, and the exact blocker boundary between repo-complete implementation work and still-pending QA sign-off | Claimed and completed during the 9:50 PM hourly kickoff after Jimmy's latest plan kept Priya on QA-facing recovery and sign-off clarity, so Priya pulled the smallest safe finishable repo-side documentation task and refreshed the active QA handoff notes to match the new frontend QA surface plus the remaining T-095 sign-off gap. |
| T-108 | Add source-email debug read endpoints so engineering and QA can inspect persisted provenance records without querying PostgreSQL manually | Sam | QA | feat/jobtrackr-source-email-debug-api | T-091, T-095 | API exposes source-email debug reads for persisted provenance records, supports the minimum filters needed for inspection, includes tests, and documents the endpoint for operator and QA use | An engineer can fetch recent `source_emails` through the API, optionally narrow to matched job emails, and verify persisted provenance fields without opening the database directly or reconstructing records from sync logs | Claimed during the 10:10 PM hourly kickoff after Jimmy's latest plan kept Frank on backend/shared-contract follow-through and the active table had no genuinely unclaimed Frank-sized task left, so Frank pulled the smallest safe finishable provenance-inspection gap. Implementation completed the same hour by adding `GET /api/v1/source-emails` with `limit`, `matchedAsJob`, `fromEmail`, and `search` filters, wiring the Postgres read path, adding HTTP coverage in `apps/api/internal/http/source_emails_test.go`, updating `apps/api/README.md`, and re-running `go test ./...` plus `go build ./...` in `apps/api` before handing the ticket to Sam for QA. |
| T-109 | Refresh kickoff-facing frontend recovery notes so live entrypoints route Marcus pickup through the T-106/T-107 server-backed workspace handoff before the older continuity contracts | Marcus | completed | chore/jobtrackr-frontend-continuity-handoff-refresh | T-105, T-106, T-107 | README, PROJECT, and the Phase 3 handoff package call out T-106 as the live frontend handoff, T-107 as the current QA summary, and keep the canonical detail-view, list-to-detail, and workspace continuity contracts as the next recovery layer for frontend continuity work | An engineer recovering from an hourly kickoff can open the live entrypoints, see the current server-backed workspace handoff first, understand the remaining T-095 plus stale-path blocker story, and then continue into the canonical frontend continuity docs without relying on older preserved guidance | Claimed and completed during the 11:06 PM hourly kickoff after Jimmy's latest plan emphasized T-106/T-107 as the live frontend handoff state; Marcus updated the live kickoff-facing entrypoints on `chore/jobtrackr-frontend-continuity-handoff-refresh` so frontend recovery now starts from the server-backed workspace handoff and current QA summary before falling through to the canonical continuity contracts. |
| T-110 | Refresh roadmap-facing kickoff guidance so ROADMAP and the PM decision memo point to the live T-106/T-107 frontend handoff and the current remaining QA blocker story | Frank | completed | chore/jobtrackr-roadmap-handoff-refresh | T-083, T-109 | ROADMAP and the PM decision memo call out T-106 as the live frontend handoff, T-107 as the current QA summary, and the remaining blocker boundary as T-095 fixture-backed validation plus stale external kickoff drift | Engineers opening roadmap-facing planning docs can recover to the same current handoff state they see in README, PROJECT, and the Phase 3 handoff package, without losing the narrowed blocker story or the current frontend continuity entrypoint | Claimed and completed during the 12:10 AM hourly kickoff after Jimmy's latest plan confirmed the live frontend handoff had moved to T-106 and T-107 while roadmap-facing docs still stopped short of that current state. |
| T-114 | Byte-align the live frontend recovery note across README, PROJECT, and the Phase 3 handoff package so hourly kickoff points to the same T-106/T-107 handoff, canonical continuity contracts, and ordered project-root files | Marcus | completed | chore/jobtrackr-live-frontend-recovery-note | T-109 | README, PROJECT, and the handoff package reuse one live frontend recovery note with the same project-root recovery order, T-106/T-107 handoff callouts, and canonical detail/list/workspace continuity pointers | Engineers opening any live kickoff-facing entrypoint can recover from stale hourly prompts using the same ordered `projects/jobtrackr/...` files, see the same current frontend handoff checkpoints, and land on the same canonical continuity docs without wording drift | Claimed and completed during the 2:40 AM hourly kickoff after Jimmy's latest plan kept Marcus on frontend continuity and the active table had no realistic unclaimed Marcus-sized task left, so Marcus pulled the smallest safe finishable live-entrypoint alignment pass and byte-aligned the shared recovery note across `projects/jobtrackr/README.md`, `projects/jobtrackr/PROJECT.md`, and `projects/jobtrackr/specs/jobtrackr-phase-3-engineering-handoff-package-2026-04-20.md`. |
| T-115 | Refresh the live dashboard shell copy so the protected frontend no longer implies mock-only data now that T-106 hands off a server-backed workspace with safe fallback visibility | Alice | completed | chore/jobtrackr-dashboard-live-copy | T-106 | Dashboard copy, README notes, and implementation-facing handoff wording describe the current server-backed persisted-jobs slice accurately while preserving mock fallback behavior, with validation rerun on the web app | Engineers and reviewers opening the dashboard or frontend README can see that the live slice prefers persisted jobs API data with safe mock fallback, and the web app still passes its local test/build checks after the copy refresh | Claimed during the 3:55 AM hourly kickoff after Jimmy's latest plan kept the live frontend lane centered on T-106/T-107, then completed the same session by restoring the canonical jobs workspace shell, refreshing the dashboard plus frontend README copy to describe the live persisted-jobs-first slice, adding shared workflow/fit presentation helpers with tests, and rerunning `npm test` plus `npm run build` in `apps/web`. |
| T-116 | Publish a T-106 QA sign-off checklist so Sam can validate the server-backed dashboard and jobs workspace handoff from one implementation-facing note | Priya | completed | chore/jobtrackr-t106-signoff-checklist | T-106, T-107 | QA has one checklist that links the live frontend handoff, repo-side verification commands, API-up persisted-jobs checks, API-down mock fallback checks, and fallback-reason assertions for pending-fit and workflow normalization | Sam can open one sign-off note, run the documented web verification and UI walkthrough steps, and understand exactly what still blocks moving T-106 from QA to Completed | Claimed and completed during the 4:23 AM hourly kickoff after Jimmy's latest plan kept QA narrowed to T-095 sign-off plus T-106 frontend handoff verification, so Priya pulled the smallest safe finishable repo-side QA task and published a dedicated T-106 sign-off checklist for the server-backed workspace slice. |
| T-117 | Clarify hourly ticket-source recovery so kickoff work can safely continue when GitHub is the long-term tracker but `DEVELOPMENT_PLAN.md` still drives active claim flow | Marcus | completed | chore/jobtrackr-ticket-source-recovery | — | README and DEVELOPMENT_PLAN explain that hourly recovery still starts from the project-root markdown handoff set, and they define how to proceed when no genuinely unclaimed plan row remains for the current lane | An engineer following an hourly kickoff can distinguish durable GitHub issue tracking from the live markdown recovery ledger, avoid hijacking QA-owned or completed tasks, and add the smallest safe follow-through task explicitly when the plan has no unclaimed row left | Claimed and completed during the 4:40 AM hourly kickoff after the stale prompt still routed pickup through DEVELOPMENT_PLAN even though the live table had no unclaimed row left; Marcus added explicit recovery guidance so future hourly pickups can create and finish the next smallest safe follow-through task without faking ownership on QA or completed work. |
| T-118 | Add backend/shared-contract provenance recovery pointers so kickoff docs route Frank pickup through the live T-091, T-095, and T-108 source-email handoff set | Frank | completed | chore/jobtrackr-backend-provenance-recovery | T-091, T-095, T-108 | README, PROJECT, and the Phase 3 handoff package call out the backend/shared-contract recovery order for source-email provenance work, keep T-091 as the live Frank lane, and point QA/debug follow-through at the current T-095 and T-108 handoff docs | An engineer recovering Frank's lane from an hourly kickoff can move from the live project-root files into the source-email persistence sign-off checklist, evidence bundle, and debug-read endpoint coverage without guessing which provenance docs are current | Claimed and completed during the 6:10 AM hourly kickoff after Jimmy's latest plan kept Frank on backend/shared-contract follow-through around T-091 and the active table had no genuinely unclaimed Frank-sized row left, so Frank pulled the smallest safe finishable provenance-recovery documentation task and aligned the live kickoff entrypoints to the current source-email handoff set. |
| T-119 | Byte-align preserved implementation-slice wording so preserved handoff and QA review docs reuse the same current auth -> session -> Gmail readonly connect -> persisted jobs slice phrasing | Alice | completed | chore/jobtrackr-preserved-slice-wording | — | The preserved first-build slice, engineering handoff v1, QA testability review, and clarifications review use the same current implementation-slice wording as the live repo handoff docs, without reintroducing `persisted jobs UI lane` phrasing | Engineers opening preserved implementation-facing docs see the same current slice wording used by the live QA and kickoff recovery notes, and no preserved entrypoint implies a different implementation lane name during hourly recovery | Claimed and completed during the 6:30 AM hourly kickoff after Jimmy's latest plan kept hourly recovery centered on consistent handoff wording and the live table had no genuinely unclaimed Alice-sized row left, so Alice pulled the smallest safe finishable wording-alignment task and normalized the preserved implementation-facing docs to the live `persisted jobs slice` phrasing. |
| T-090 | Verify the resolved JobsTable compile blocker and update QA-facing blocker notes so repo docs stop treating the dashboard build as currently blocked | Sam | QA | chore/jobtrackr-kickoff-recovery-docs | T-086 | The repo-side blocker tracker and in-progress QA coverage reflect the verified green web build, preserve the external stale-path blocker as the remaining coordination gap, and point QA at the fresh verification evidence | Running the Next.js web build succeeds, the open blocker entry for the `JobsTable.tsx` JSX parse failure is moved out of Active, and QA-facing notes describe the compile issue as resolved verification evidence instead of an active engineering blocker | Claimed by Priya during hourly kickoff from an unassigned QA verification gap after the web build passed again; implementation docs updated and handed to Sam for sign-off. |

| T-088 | Fix Next.js JSX parse failure in `apps/web/components/JobsTable.tsx` caused by `Unexpected token `div`` so the jobs dashboard can compile again | Alice | in-progress | fix/jobtrackr-jobs-table-jsx-parse | — | JobsTable compiles cleanly, the Next.js build/dev server no longer throws the JSX parser error at line 44, and the dashboard table renders again in the current frontend slice | Running the web app no longer fails with `Unexpected token `div`. Expected jsx identifier`, the component compiles in TypeScript/Next.js, and the jobs table renders in the browser without syntax errors | Logged from Keith bug report on 2026-04-21 after a compile failure in `/Users/keith.goff/Documents/jobtrackr/apps/web/components/JobsTable.tsx` blocked the dashboard frontend |
| T-091 | Add source-email persistence and GOG-driven Gmail discovery flow so job-search emails can be found, stored, reprocessed, and upserted into the jobs database | Frank | in-progress | feat/jobtrackr-gog-source-email-ingestion | T-002, T-018, T-026 | Source-email schema is defined, Gmail discovery queries are documented, GOG-based discovery can persist candidate source emails, and parsed job upsert flow is explicitly wired to source-email records | Engineers can search Gmail for job-related emails via GOG, persist previously unseen messages as source-email rows, reprocess saved source emails, and upsert parsed jobs into the database without relying on ad hoc inbox-only workflows | Added from Keith request on 2026-04-22 to make GOG-based Gmail job-email discovery feed the job database explicitly |
| T-095 | Persist source-email records and email-to-job linkage during Gmail sync so ingestion stores provenance before upserting jobs | Sam | QA | feat/jobtrackr-source-email-persistence | T-091 | Gmail sync writes raw source-email rows, links persisted jobs back to their source email, includes tests, and documents the current persistence behavior | Running the sync flow persists each Gmail message once in `source_emails`, writes a row to `job_source_emails` for saved jobs, and the API README describes the provenance-first ingestion slice | Claimed during hourly kickoff because no unclaimed active ticket remained and the repo lacked the smallest finishable source-email persistence slice; implementation is complete on `feat/jobtrackr-source-email-persistence`, and local `go test ./...` plus `go build ./...` both passed on 2026-04-22 while the task waits for Sam QA sign-off. |
| T-099 | Attach a QA evidence bundle for source-email persistence so Sam can validate T-095 without reconstructing proof from commit history | Priya | completed | chore/jobtrackr-kickoff-recovery-docs | T-095 | QA-facing docs link the current source-email persistence branch, commit, rerun command output, and code/test/readme evidence for provenance-first sync behavior | Sam can open one repo-side evidence note, re-run the same verification commands, and jump directly to the service, regression test, and API README that prove source-email persistence plus job linkage | Claimed during the 12:50 PM hourly kickoff after QA coverage still flagged missing evidence for T-095; completed by adding a dedicated evidence note, linking it from QA docs, and rerunning `go test ./...` plus `go build ./...` on `feat/jobtrackr-source-email-persistence`. |
| T-100 | Add a canonical GOG discovery helper and operator-facing docs so source-email ingestion work does not stop at opaque Gmail API query strings | Frank | completed | feat/jobtrackr-gog-discovery-recovery | T-091, T-095 | Repo includes one reusable GOG Gmail search helper, README/API docs explain when to use it, and the ingestion spec distinguishes read-only discovery from the persistence path | An engineer can run one documented helper to inspect the live Gmail query with `gog`, understand that discovery is read-only, and then use the API sync path to persist `source_emails`, `jobs`, and `job_source_emails` without reconstructing the flow from scattered notes | Claimed during the 2:10 PM hourly kickoff because no unclaimed active task remained in the table and Jimmy explicitly prioritized making the provenance-first ingestion story impossible to misread; completed by adding the helper, documenting it in the repo README plus API README, and updating the Gmail ingestion spec to call out discovery-vs-persistence behavior. |
| T-101 | Finish the mock detail-view editing slice so notes and tags use explicit save/cancel interactions across the jobs drawer and full-page detail view | Marcus | completed | feat/jobtrackr-detail-editing-finish | T-012, T-019, T-030 | The shared detail panel supports in-place note drafts, tag add/remove controls, explicit save/cancel affordances, local helper coverage, and README guidance that the current slice is wired for mock editing while API persistence is still pending | An engineer can open the jobs workspace or full-page detail view, change notes or tags with the same interaction pattern, see deterministic draft state without autosave ambiguity, and run frontend tests/build successfully against the updated mock detail panel | Claimed during the 2:40 PM hourly kickoff because no unclaimed Marcus task remained in the active table and Jimmy's latest plan kept Marcus on frontend detail and contract continuity work; completed by turning the shared detail panel into a client-side draft editor, adding tag-draft helpers plus tests, updating the frontend README status note, and re-running the web test/build checks. |
| T-102 | Publish a T-095 QA sign-off checklist so Sam can validate source-email persistence from one implementation-facing note without reconstructing evidence or fixture steps | Priya | completed | chore/jobtrackr-t095-signoff-checklist | T-095, T-099 | QA has one checklist that links the evidence bundle, fixture-backed verification steps, expected database assertions, and the current remaining gap for Sam sign-off | Sam can open one sign-off note, run the documented validation steps, confirm `source_emails` and `job_source_emails` outcomes, and understand exactly what still blocks moving T-095 from QA to Completed | Claimed during the 3:50 PM hourly kickoff after Jimmy kept Priya focused on QA-facing validation and recovery-note consistency, then completed by publishing `jobtrackr-source-email-persistence-signoff-checklist-2026-04-22.md` and wiring it into the blocker checklist, blocker summary, and active QA coverage so the T-095 sign-off path now lives in one QA-facing note. |

## Completed
| ID | Task | Owner | Merged |
|---|---|---|---|
| T-089 | Restore the jobs table to the canonical workflow and fit-signal model while keeping the JSX compile fix intact | Sam | No |
| T-093 | Reconcile blocker-tracking docs so the JobsTable compile issue is shown as a QA verification item instead of an open engineering blocker | Frank | No |
| T-094 | Align PROJECT lane ownership and recovery guidance with the README and Phase 3 handoff package so kickoff-facing docs keep the same current pickup story | Alice | No |
| T-085 | Byte-align kickoff-facing recovery order and lane wording so README, PROJECT, and the Phase 3 handoff package all use the same explicit project-root sequence and current frontend split | Marcus | No |
| T-090 | Verify the resolved JobsTable compile blocker and update QA-facing blocker notes so repo docs stop treating the dashboard build as currently blocked | Priya | No |
| T-096 | Normalize duplicate task IDs so QA traceability and dependency references no longer collide between blocker verification and source-email ingestion work | Alice | No |
| T-097 | Clarify GOG-driven Gmail discovery and source-email persistence requirements so engineering pickup does not stop at inbox search only | Frank | No |
| T-098 | Byte-align the preserved first-build slice frontend-ownership note with engineering handoff v1 and the QA testability review so Marcus recovery docs stop drifting on the current frontend split | Marcus | No |
| T-086 | Refresh QA hourly coverage and blocker notes so Priya's implementation-facing test docs reflect the latest Jimmy fetch, current external kickoff drift, and the still-live QA doc-alignment lane | Priya | No |
| T-099 | Attach a QA evidence bundle for source-email persistence so Sam can validate T-095 without reconstructing proof from commit history | Priya | No |
| T-100 | Add a canonical GOG discovery helper and operator-facing docs so source-email ingestion work does not stop at opaque Gmail API query strings | Frank | No |
| T-101 | Finish the mock detail-view editing slice so notes and tags use explicit save/cancel interactions across the jobs drawer and full-page detail view | Marcus | No |
| T-102 | Publish a T-095 QA sign-off checklist so Sam can validate source-email persistence from one implementation-facing note without reconstructing evidence or fixture steps | Priya | No |
| T-107 | Refresh QA hourly coverage after Jimmy's latest plan so Priya's repo-facing validation notes reflect the live frontend slice handoff and current remaining QA blockers | Priya | No |
| T-075 | Normalize the shared QA recovery note across PRD v2, the blocker checklist, and in-progress QA coverage so the path-recovery order and current implementation lane wording are byte-for-byte aligned | Priya | No |
| T-080 | Normalize preserved implementation-facing recovery and lane-ownership wording so the historical first-build slice, engineering handoff v1, and QA testability review all reflect the same frontend split for Marcus and Alice and the same explicit project-root recovery order | Marcus | No |
| T-081 | Byte-align the preserved implementation-facing frontend-ownership note so the first-build slice, engineering handoff v1, and QA testability review all reuse the same wording for Marcus and Alice's current lane split | Marcus | No |
| T-084 | Byte-align preserved planning and handoff recovery notes so ROADMAP, the PM decision memo, the QA testability review, and engineering handoff v1 all use the same recovery block and current lane wording | Frank | No |
| T-076 | Normalize preserved planning and handoff recovery notes so ROADMAP, the PM memo, QA testability review, and legacy handoff docs share the same explicit recovery order and active implementation lane wording | Frank | No |
| T-083 | Byte-align roadmap-facing lane-ownership wording so ROADMAP and the PM decision memo use the same current frontend split for Alice and Marcus while keeping Frank on source-of-truth maintenance | Frank | No |
| T-103 | Reconcile preserved frontend detail-flow specs so the frontend plan and table UI spec stop drifting from the canonical detail-view and list-to-detail contracts | Frank | No |
| T-077 | Normalize the preserved engineering handoff v1 and QA testability review recovery note so both docs use the same absolute-path recovery order and current frontend-first lane wording from Jimmy's latest plan | Alice | No |
| T-082 | Byte-align the preserved implementation-facing Alice lane note so the engineering handoff v1 and QA testability review use the same frontend-focused recovery wording for Alice's current slice | Alice | No |
| T-087 | Label and verify the shared Alice frontend lane note in preserved implementation-facing docs so the handoff v1 and QA testability review expose the same byte-aligned pickup wording | Alice | No |
| T-104 | Refresh in-progress QA coverage after Jimmy's latest frontend-split update so active verification notes reflect T-103 completion, the preserved-doc regression state, and the remaining T-095 sign-off gap | Alice | No |
| T-111 | Add an explicit shared Alice lane note block to the preserved implementation-facing docs and close the verification loop in active QA coverage | Alice | No |
| T-112 | Reconcile stale active-plan traceability after the shared Alice lane note closeout so preserved-doc tasks stop showing as live work | Marcus | No |
| T-113 | Prune stale Upcoming carry-forward notes so hourly recovery points at the still-open QA surface instead of already-finished kickoff history | Priya | No |
| T-114 | Byte-align the live frontend recovery note across README, PROJECT, and the Phase 3 handoff package so hourly kickoff points to the same T-106/T-107 handoff, canonical continuity contracts, and ordered project-root files | Marcus | No |
| T-105 | Add kickoff-facing frontend continuity pointers so README, PROJECT, and the Phase 3 handoff package explicitly route frontend pickup back to the canonical detail-view, list-to-detail, and workspace continuity contracts | Marcus | No |
| T-109 | Refresh kickoff-facing frontend recovery notes so live entrypoints route Marcus pickup through the T-106/T-107 server-backed workspace handoff before the older continuity contracts | Marcus | No |
| T-110 | Refresh roadmap-facing kickoff guidance so ROADMAP and the PM decision memo point to the live T-106/T-107 frontend handoff and the current remaining QA blocker story | Frank | No |
| T-116 | Publish a T-106 QA sign-off checklist so Sam can validate the server-backed dashboard and jobs workspace handoff from one implementation-facing note | Priya | No |
| T-117 | Clarify hourly ticket-source recovery so kickoff work can safely continue when GitHub is the long-term tracker but `DEVELOPMENT_PLAN.md` still drives active claim flow | Marcus | No |
| T-118 | Add backend/shared-contract provenance recovery pointers so kickoff docs route Frank pickup through the live T-091, T-095, and T-108 source-email handoff set | Frank | No |
| T-120 | Surface the live QA sign-off entrypoints in kickoff-facing recovery notes so hourly pickup points directly at the current T-106 and T-095 validation checklists | Marcus | No |
| T-121 | Normalize the live QA recovery note and sign-off entrypoints so PRD v2, the blocker checklist, and active QA coverage all recover through PROJECT and point directly at the two open QA gates | Priya | No |
| T-122 | Normalize preserved recovery-path examples so roadmap-facing and preserved planning docs recover through the explicit `projects/jobtrackr/...` source-of-truth set and current persisted-jobs slice wording | Frank | completed | chore/jobtrackr-preserved-recovery-paths | T-073 | ROADMAP, the PM decision memo, and preserved handoff/review docs all use the same project-root recovery order, avoid dead absolute-path examples, and describe the current auth -> session -> Gmail readonly connect -> persisted jobs slice consistently | Engineers opening roadmap-facing or preserved planning docs can recover from stale hourly prompts without seeing `~/Documents/project-requirements/...` paths, and they land on the same live project-root source files plus persisted-jobs slice wording used by the current kickoff package | Claimed and completed during the 8:10 AM hourly kickoff after Jimmy's latest plan confirmed there was no genuinely unclaimed Frank row left in the active table; Frank pulled the smallest safe follow-through task and normalized the preserved recovery notes across roadmap-facing and preserved planning docs to the live `projects/jobtrackr/...` source set and current persisted-jobs slice wording. |
| T-123 | Normalize the live handoff-package recovery wording so project-root path examples and current slice phrasing stay aligned with README and PROJECT | Marcus | completed | chore/jobtrackr-live-handoff-package-alignment | T-114, T-117 | The live Phase 3 handoff package uses the same project-root recovery examples and current Next.js web plus Go API slice wording already established in README and PROJECT, without reintroducing absolute workspace paths or `persisted jobs UI` phrasing | Engineers opening the live handoff package can recover using `projects/jobtrackr/...` paths only, and the current implementation slice is described as Google auth -> session -> Gmail readonly connect -> persisted jobs retrieval instead of older wording that drifts from the other kickoff entrypoints | Claimed and completed during the 8:40 AM hourly kickoff after Jimmy's latest plan kept Marcus on frontend continuity and the active table still had no genuinely unclaimed Marcus row; Marcus pulled the smallest safe live-entrypoint cleanup, aligned the remaining Phase 3 handoff package recovery wording to README and PROJECT, and removed the last live kickoff-package use of an absolute PROJECT path plus older `persisted jobs UI` slice wording. |
| T-124 | Surface the live QA gate pair in kickoff-facing recovery notes so hourly pickup immediately sees the current T-106 and T-095 sign-off entrypoints plus the stale-path recovery rule from Jimmy's latest plan | Alice | completed | chore/jobtrackr-live-qa-gate-recovery | — | README, PROJECT, the live handoff package, and DEVELOPMENT_PLAN all call out the same current QA gate pair, sign-off checklist entrypoints, and stale root-path recovery rule after Jimmy's latest hourly guidance | Engineers recovering from an hourly kickoff can immediately see that the live QA surface is T-106 plus T-095, open the matching sign-off checklists without repo archaeology, and treat `~/Documents/project-requirements/DEVELOPMENT_PLAN.md` as external prompt drift rather than a live repo path | Claimed and completed during the 9:30 AM hourly kickoff after Jimmy's latest plan reconfirmed the two real QA gates and the stale root-path drift; Alice pulled the smallest safe follow-through task, aligned README, PROJECT, and the live Phase 3 handoff package to the same QA-gate wording, and updated DEVELOPMENT_PLAN so future hourly recovery lands on the same entrypoints immediately. |
| T-125 | Attach the concrete T-106 evidence commit and validation pointers so QA can sign off the persisted-jobs workspace handoff without branch archaeology | Priya | completed | chore/jobtrackr-live-qa-gate-recovery | T-106, T-116 | The T-106 sign-off checklist names the concrete implementation evidence commit, the active QA coverage reflects that proof point, and the live plan records the repo-side handoff update without reopening implementation scope | Sam can open the persisted-jobs workspace sign-off note, find the exact commit that landed the implementation proof, and continue QA from one deterministic frontend evidence path instead of reconstructing branch history | Claimed and completed during the 9:50 AM hourly kickoff after Jimmy's latest plan kept the live QA surface narrowed to T-106 and T-095, while the T-106 checklist still showed `unknown in repo docs` for the evidence commit; Priya pulled the smallest safe follow-through task, pinned the concrete frontend evidence commit, refreshed the active QA coverage note, and kept the remaining blocker boundary limited to executed QA sign-off rather than repo-side documentation drift. |
| T-126 | Normalize QA sign-off checklist path pointers so T-095 and T-106 validation docs use project-root repo references instead of machine-specific absolute workspace paths | Frank | completed | chore/jobtrackr-qa-signoff-path-normalization | T-102, T-116, T-124, T-125 | The two live QA sign-off checklists use project-root repo paths for code/doc pointers and verification entrypoints, and the live plan records the recovery-safe doc cleanup explicitly | Sam can follow the T-095 and T-106 implementation-facing checklists without inheriting `~/Documents/...` machine assumptions for repo docs or code pointers, while the existing recovery order and evidence references remain intact | Claimed and completed during the 10:10 AM hourly kickoff after Jimmy's latest plan reconfirmed T-095 and T-106 as the two live QA gates and no genuinely unclaimed Frank-sized row remained, so Frank pulled the smallest safe repo-side follow-through and replaced machine-specific absolute pointers in both sign-off checklists with repo-root `apps/...` and `projects/jobtrackr/...` references. |

## Upcoming
This section now tracks only still-open carry-forward items. Older kickoff claim/completion history lives in the task tables above.

- T-095 remains the only live QA sign-off gate in repo docs. Use `projects/jobtrackr/specs/jobtrackr-source-email-persistence-signoff-checklist-2026-04-22.md` plus `projects/jobtrackr/specs/jobtrackr-source-email-persistence-evidence-2026-04-22.md` for the current fixture-backed validation path.
- T-106 remains in QA as the live frontend handoff target for the server-backed dashboard and jobs workspace slice. Use `projects/jobtrackr/specs/jobtrackr-persisted-jobs-workspace-signoff-checklist-2026-04-23.md` as the primary QA path before moving it from QA to Completed, with implementation evidence anchored to commit `858127d` on `feat/jobtrackr-persisted-jobs-workspace`.
- The live QA gate pair is T-106 plus T-095. Hourly recovery should surface both sign-off entrypoints together so frontend and provenance validation stay visible in the same pickup pass.
- External hourly prompts may still reference `~/Documents/project-requirements/DEVELOPMENT_PLAN.md`; treat that as stale coordination drift and recover to `projects/jobtrackr/DEVELOPMENT_PLAN.md`, `projects/jobtrackr/PROJECT.md`, and `projects/jobtrackr/specs/jobtrackr-phase-3-engineering-handoff-package-2026-04-20.md`.
- If the live table has no truly unclaimed row for the current lane, add the smallest safe follow-through task explicitly before claiming it rather than taking over a QA-owned or already-completed task.
- Historical kickoff pull-forward notes before T-113 are intentionally pruned here because they were already captured in Active or Completed task history and were obscuring the remaining open recovery surface.

## Decisions & Notes
- Keep task ownership explicit to avoid duplicate work across SWE agents.
- Hourly recovery still starts from the project-root markdown handoff set even while GitHub issues remain the longer-term ticket system, because automation-facing prompts and implementation-facing notes still recover through `DEVELOPMENT_PLAN.md` first.
- When the active table has no genuinely unclaimed task for the current lane, create the smallest safe follow-through task explicitly and claim that new row instead of reusing a QA-owned or completed task.
- Branch naming should track the task focus and remain one task per branch.
- Gmail integration for the new job search project will use Gmail API with local web OAuth flow and env-based secrets, not embedded credentials.
- Gmail access remains Gmail readonly scope only for MVP.
- GOG is the preferred operator workflow for searching Gmail job-alert emails and validating ingestion queries during MVP development.
- The canonical operator entrypoint for that discovery step is `apps/api/scripts/gog-discover-job-emails.sh`, which should mirror the same query defaults as API sync while remaining a read-only debug path.
- The ingestion pipeline must not stop at email search. Relevant Gmail messages should be captured as raw source-email records, parsed into candidate jobs, deduplicated, and persisted into the jobs database with source-email linkage.
- Source-email persistence should store Gmail message id, thread id, sender, subject, received timestamp, labels, raw or normalized message body, parse status, and processed timestamp.
- Parsed jobs should retain a linkage back to one or more source emails so repeated alerts can merge onto one job record without losing provenance.
- Google auth policy: allow anyone with a Google account to sign in (not single approved account only).
- The first Alice vertical slice is auth, protected shell, Gmail connection, and raw source-email ingestion before fit analysis.
- Initial sync defaults to the last 30 days, with future work planned around a 10 minute incremental sync plus manual refresh.
- Frank is owning the dependency map and milestone sequencing pass to identify blockers before Milestones 1 through 4 proceed in parallel.
- Marcus is owning the jobs dashboard contract shape so frontend and API work can converge on the same table payload, especially around match rating, low-fit indicator semantics, and non-workflow table signals.
- Job enrichment: scrape full details from job detail page linked in email.
- Deduplication: match on job URL to merge repeated alerts for the same job.
- Mandatory job record fields: id, title, company, description, salary range, location, fit score (nullable until analysis), skills, tags.
- Fit display: show a user-facing match rating when fit analysis exists, while keeping fit state separate from workflow status.
- Low-fit handling: jobs with fit score below 60 must follow the canonical low-fit contract and must not introduce `not a match` as a workflow status.
- Dashboard semantics: match rating and low-fit indicators are fit signals, not workflow states, and table payload wording should reflect that distinction.
- Notifications: no email/push notifications; user reviews jobs in UI.
- Status model: new, interested, applied, interviewing, offer, rejected.

## Branch & PR Rules
- Branch naming: `feat/<short-desc>`, `fix/<short-desc>`, `chore/<short-desc>`
- One task per branch
- PR must reference the Task ID in the title
- Do not merge without Sam (QA) sign-off on feature work

## Task Workflow
- When SWE completes DoD for a task, move task status to "QA" and assign to Sam
- Sam (QA) validates DoD criteria are met before approving PR
- If DoD not met, Sam returns task to SWE with feedback
- Only after QA approval can task be moved to "Completed"
- If no genuinely unclaimed task remains in the active table for the current lane, add the next smallest safe follow-through task explicitly before claiming it, so the plan remains the honest hourly recovery ledger

## Development Steps

### Milestone 1: Auth Foundation
**Goal:** Ship user login and protected app access.

**Locked product semantics for this milestone:**
- Google federated login
- protected dashboard access
- single jobs workspace entry via authenticated session

**Steps:**
1. Set up monorepo structure: `apps/web`, `apps/api`, `packages/types`, `infra/`
2. Initialize Go API with Gin framework
3. Initialize Next.js web app with TypeScript
4. Configure Google OAuth credentials (client ID, secret, redirect URIs)
5. Implement OAuth initiation endpoint (`GET /api/v1/auth/google`)
6. Implement OAuth callback handler (`GET /api/v1/auth/callback`)
7. Create user record in database on successful OAuth
8. Implement secure session cookie creation (HTTP-only, secure, same-site)
9. Add protected route middleware to validate session
10. Build login page in Next.js with Google sign-in button
11. Add session-aware API client to frontend
12. Test end-to-end sign-in flow
13. Verify unauthenticated users cannot access dashboard
14. Verify authenticated users reach dashboard shell

**Exit Criteria:**
- User can sign in with Google
- Unauthenticated users cannot access dashboard
- Authenticated users reach the dashboard shell

---

### Milestone 2: Dashboard Shell and Table Foundation
**Goal:** Ship the visible frontend shell for the app.

**Locked product semantics for this milestone:**
- dashboard is the main jobs page
- jobs page is a searchable table
- required columns include fit flag, match %, company, title, location, source, date found, and status
- use mock data first only to unblock UI implementation, then replace with DB-backed data

**Steps:**
1. Design dashboard layout (header, filters row, jobs table, pagination)
2. Implement dashboard page in Next.js
3. Add header with account controls and logout
4. Create filters row shell (status, location, date range, search)
5. Build jobs table component with required columns (company, title, location, source, date found, match rating, status)
6. Implement row interaction pattern (click to expand detail drawer)
7. Add loading states for table
8. Add empty states for no jobs
9. Create mock job data for testing
10. Connect table to mock data
11. Test responsive layout
12. Verify table renders all required columns
13. Verify row interaction works

**Exit Criteria:**
- Authenticated user sees dashboard
- Table renders required columns
- Row interaction pattern is defined and working

---

### Milestone 3: Data Model and Real Job List
**Goal:** Replace mock table data with real persisted jobs.

**Locked product semantics for this milestone:**
- jobs page reads from the database
- searchable table remains the primary review UI
- payload must include match percentage when available

**Steps:**
1. Define database schema (users, jobs, tags, notes, gmail_tokens, sync_state)
2. Create PostgreSQL migration scripts
3. Set up local PostgreSQL with Docker Compose
4. Implement sqlc for type-safe database queries
5. Create job repository with CRUD operations
6. Implement jobs list query with search, filters, sorting, and pagination
7. Implement single-job detail query
8. Add API endpoints: `GET /api/v1/jobs`, `GET /api/v1/jobs/:id`
9. Connect frontend dashboard to real API endpoints
10. Test with seed job data
11. Verify dashboard reads jobs from database
12. Verify job detail view can load a real job

**Exit Criteria:**
- Dashboard reads jobs from database
- Job detail view can load a real job

---

### Milestone 4: Gmail Ingestion Baseline
**Goal:** Populate real jobs from Gmail inbox scanning.

**Locked product semantics for this milestone:**
- GOG-powered Gmail search is the preferred operator path for finding job-related emails and validating search queries during MVP development
- ingestion must persist raw source-email records before or alongside parsed job creation
- parsed jobs must keep provenance back to source emails
- duplicate alerts for the same job should merge onto one job record without losing email history

**Steps:**
1. Configure Gmail API OAuth scope (readonly)
2. Implement Gmail token storage (encrypted at rest)
3. Add Gmail token refresh logic
4. Build Gmail client wrapper for API calls
5. Install and validate the GOG Gmail workflow used for operator search, debugging, and ingestion-query development
6. Define canonical GOG Gmail search queries for job-alert discovery (sender, subject, label, and date-window patterns)
7. Implement scheduled polling worker (configurable interval)
8. Fetch recent messages from Gmail (incremental from last checkpoint)
9. Implement job email detection rules (sender, subject keywords, labels)
10. Persist raw source-email records including Gmail message id, thread id, sender, subject, labels, received timestamp, and raw or normalized body content
11. Build rule-based job parser (extract title, company, location, link, salary)
12. Implement deduplication logic (match on job URL)
13. Create or update job records from parsed data
14. Persist email-to-job linkage so one job can trace back to one or more source emails
15. Add ingestion logging, parse status tracking, and error handling
16. Test sync with test Gmail account
17. Verify GOG search queries return the expected job-alert emails
18. Verify inbox scan creates job records from relevant emails
19. Verify duplicate obvious repeats do not create duplicate active rows
20. Verify raw source-email records and source-email linkage are queryable for debugging and review

**Exit Criteria:**
- GOG search queries reliably find relevant job-alert emails
- Inbox scan creates job records from relevant emails
- Duplicate obvious repeats do not create duplicate active rows
- Raw source-email records and email-to-job linkage are persisted for debugging and provenance

---

### Milestone 5: Resume Upload and Fit Foundation
**Goal:** Allow the system to evaluate jobs against the user's resume.

**Steps:**
1. Design resume upload UI in settings page
2. Implement file upload endpoint (`POST /api/v1/resume`)
3. Add resume storage (file system or object storage)
4. Parse uploaded resume (extract text, skills, experience)
5. Store parsed resume profile in database
6. Implement active resume selection logic
7. Wire fit analysis trigger (run when new jobs are created)
8. Add resume display in settings
9. Test resume upload and parsing
10. Verify user can upload a resume
11. Verify uploaded resume is available for job fit analysis

**Exit Criteria:**
- User can upload a resume
- Uploaded resume is available for job fit analysis

---

### Milestone 6: Fit Analysis and Fit Signals
**Goal:** Surface likely good-fit roles in the dashboard without conflating fit indicators with workflow state.

**Steps:**
1. Design fit scoring algorithm (compare job vs resume skills/experience)
2. Implement fit scoring service (score 0-100)
3. Add fit analysis trigger on job creation
4. Store nullable fit score, low-fit indicator metadata, and rationale in the job record
5. Implement canonical low-fit handling for jobs below 60 without writing a non-canonical workflow status
6. Add low-fit indicator logic as a fit signal, not a workflow status
7. Display fit score in the table column as the user-facing match rating
8. Add fit badge/color coding (high score vs low score)
9. Show fit rationale in the job detail drawer
10. Test fit analysis with sample jobs and resume
11. Verify jobs receive fit results
12. Verify the dashboard clearly shows match rating, low-fit signals, and pending-fit states without implying a workflow transition
13. Verify low-fit handling works for jobs below 60 score without mutating workflow state incorrectly

**Exit Criteria:**
- Jobs receive fit results
- Dashboard clearly shows fit indicators without treating them as workflow states
- Low-fit handling works for jobs below 60 score under the canonical contract

---

### Milestone 7: Workflow Completion
**Goal:** Complete the core triage and review workflow.

**Steps:**
1. Implement job detail drawer or detail page with full job information
2. Display job description, skills, tags, notes, and source link on details page
3. Add status update endpoint (`PATCH /api/v1/jobs/:id/status`)
4. Add tag management endpoints (CRUD)
5. Add notes endpoint (CRUD)
6. Implement filter refinements (by status, fit score, date, tags)
7. Add source link access (open job URL in new tab)
8. Add better error states and retry logic
9. Implement job archival logic
10. Add search across title, company, description
11. Implement view all jobs page with full job details and advanced filtering
12. Test end-to-end workflow (login → view jobs → view details → update status → add tags)
13. Verify user can review, inspect, and update jobs end to end

**Exit Criteria:**
- User can review, inspect, and update jobs end to end

---

### Milestone 8: QA and Release Readiness
**Goal:** Prepare MVP for real use.

**Steps:**
1. Review all features against PRD and specs
2. Run acceptance tests for each milestone
3. Fix critical bugs identified during testing
4. Validate edge cases (empty inbox, parsing failures, token expiry)
5. Test with real Gmail account and job alerts
6. Verify Gmail token refresh works
7. Test resume upload with various file formats
8. Validate fit analysis accuracy
9. Create launch checklist
10. Set up staging environment
11. Deploy to staging for final validation
12. Prepare production deployment
13. Configure production environment variables
14. Set up monitoring (Sentry, health endpoints)
15. Verify critical user flow works from login to fit-scored jobs in the dashboard
16. Resolve major known blockers

**Exit Criteria:**
- Critical user flow works from login to fit-scored jobs in the dashboard
- Major known blockers resolved
